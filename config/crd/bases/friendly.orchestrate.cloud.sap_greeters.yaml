---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: greeters.friendly.orchestrate.cloud.sap
spec:
  group: friendly.orchestrate.cloud.sap
  names:
    kind: Greeter
    listKind: GreeterList
    plural: greeters
    singular: greeter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Greeter is the Schema for the greeters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GreeterSpec defines the desired state of Greeter.
            properties:
              people:
                type: string
            type: object
          status:
            description: GreeterStatus defines the observed state of Greeter.
            properties:
              amount_of_greetings:
                format: int64
                type: integer
              last_greeting:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: false
    subresources:
      status: {}
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: Greeter is the Schema for the greeters API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GreeterSpec defines the desired state of Greeter.
            properties:
              foo:
                description: Foo is an example field of Greeter. Edit greeter_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: GreeterStatus defines the observed state of Greeter.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
